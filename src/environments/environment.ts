/** Backend API — matches TicketManagement.API launchSettings (http profile). */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5005/api',
  appName: 'NexusDesk',
  tagline: 'Smart Internal Ticket & Incident Management',
};
